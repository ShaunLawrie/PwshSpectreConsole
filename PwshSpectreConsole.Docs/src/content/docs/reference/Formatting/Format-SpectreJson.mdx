---
sidebar:
  badge:
    text: Updated
    variant: note
title: Format-SpectreJson
---







import Asciinema from '../../../../components/Asciinema.astro'
import formatspectrejsonExample1 from '../../../../assets/examples/formatspectrejsonExample1.cast?url'
import formatspectrejsonExample2 from '../../../../assets/examples/formatspectrejsonExample2.cast?url'

### Description

This function takes an objects and converts them into syntax highlighted Json using the Spectre Console Json Library.  
Thanks to [trackd](https://github.com/trackd) for adding this!  
See https://spectreconsole.net/widgets/json for more information.



---


### Examples


**Example 1**  
This example demonstrates how to format an array of objects into a Spectre Console Json object with syntax highlighting.



```powershell
$data = @(
    [pscustomobject]@{
        Name = "John"
        Age = 25
        City = "New York"
        IsEmployed = $true
        Salary = 10
        Hobbies = @("Reading", "Swimming")
        Address = @{
            Street = "123 Main St"
            ZipCode = $null
        }
    }
)
Format-SpectreJson -Data $data
```
<Asciinema
    src={formatspectrejsonExample1}
    settings={{
        loop: false,
        terminalLineHeight: 1.1,
        theme: "spectre",
        fit: "none",
        terminalFontFamily: "'Cascadia Code', monospace"
    }}
/>


**Example 2**  
This example demonstrates how to display the contents of a JSON file using Format-SpectreJson.  
First, create a temporary JSON file for demonstration purposes.  



```powershell
$tempJson = Join-Path $env:TEMP 'example.json'
@{
    Name = 'Alice'
    Age = 30
    Skills = @('PowerShell', 'Spectre.Console')
} | ConvertTo-Json | Set-Content -Path $tempJson
# Display the JSON file using Get-Content and Format-SpectreJson
Get-Content -Raw $tempJson | Format-SpectreJson
# Type "↲" to accept the prompt

# You can also use Get-ChildItem to pass the file directly:
Get-ChildItem $tempJson | Format-SpectreJson
# Type "↲" to accept the prompt
```
<Asciinema
    src={formatspectrejsonExample2}
    settings={{
        loop: false,
        terminalLineHeight: 1.1,
        theme: "spectre",
        fit: "none",
        terminalFontFamily: "'Cascadia Code', monospace"
    }}
/>


---


### Parameters
#### **Data**

The array of objects to be formatted into Json.






|Type      |Required|Position|PipelineInput |
|----------|--------|--------|--------------|
|`[Object]`|true    |1       |true (ByValue)|



#### **Depth**

The maximum depth of the Json. Default is defined by the version of powershell.






|Type     |Required|Position|PipelineInput|
|---------|--------|--------|-------------|
|`[Int32]`|false   |2       |false        |



#### **NoBorder**

:::caution
This parameter is deprecated and will be removed in the future. It takes no effect from version 2.0.
The json output already has no border.
:::






|Type      |Required|Position|PipelineInput|
|----------|--------|--------|-------------|
|`[Switch]`|false   |named   |false        |



#### **Border**

:::caution
This parameter is deprecated and will be removed in the future. It takes no effect from version 2.0.
To add a border wrap this object in a panel, use `$data | Format-SpectreJson | Format-SpectrePanel`.
:::






|Type      |Required|Position|PipelineInput|
|----------|--------|--------|-------------|
|`[String]`|false   |3       |false        |



#### **Color**

:::caution
This parameter is deprecated and will be removed in the future. It takes no effect from version 2.0.
To add a border with a color use `$data | Format-SpectreJson | Format-SpectrePanel -Color "color"`.
:::






|Type      |Required|Position|PipelineInput|
|----------|--------|--------|-------------|
|`[String]`|false   |4       |false        |



#### **Title**

:::caution
This parameter is deprecated and will be removed in the future. It takes no effect from version 2.0.
To add a border with a title to the json data, use `$data | Format-SpectreJson | Format-SpectrePanel -Header "title"`.
:::






|Type      |Required|Position|PipelineInput|
|----------|--------|--------|-------------|
|`[String]`|false   |5       |false        |



#### **Width**

:::caution
This parameter is deprecated and will be removed in the future. It takes no effect from version 2.0.
To add a border with a width, use `$data | Format-SpectreJson | Format-SpectrePanel -Width 20`.
:::






|Type     |Required|Position|PipelineInput|
|---------|--------|--------|-------------|
|`[Int32]`|false   |6       |false        |



#### **Height**

:::caution
This parameter is deprecated and will be removed in the future. It takes no effect from version 2.0.
To add a border with a width, use `$data | Format-SpectreJson | Format-SpectrePanel -Height 20`.
:::






|Type     |Required|Position|PipelineInput|
|---------|--------|--------|-------------|
|`[Int32]`|false   |7       |false        |



#### **JsonStyle**

A hashtable of Spectre Console color names and values to style the Json output.
e.g.
```
@{
    MemberStyle    = "Yellow"
    BracesStyle    = "Red"
    BracketsStyle  = "Orange1"
    ColonStyle     = "White"
    CommaStyle     = "White"
    StringStyle    = "White"
    NumberStyle    = "Red"
    BooleanStyle   = "LightSkyBlue1"
    NullStyle      = "Gray"
}
```






|Type         |Required|Position|PipelineInput|
|-------------|--------|--------|-------------|
|`[Hashtable]`|false   |8       |false        |





---


### Syntax
```powershell
Format-SpectreJson [-Data] <Object> [[-Depth] <Int32>] [-NoBorder] [[-Border] <String>] [[-Color] <String>] [[-Title] <String>] [[-Width] <Int32>] [[-Height] <Int32>] [[-JsonStyle] <Hashtable>] [<CommonParameters>]
```
